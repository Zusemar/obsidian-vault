#golang #compiler #runtime

## Связанные темы

- [[go escape analysis]] — анализ побегов встроен в компилятор (`cmd/compile/internal/escape`)
- [[go scheduler]] — ключевое слово `go` компилятор заменяет на вызов `runtime.newproc`
- [[go goroutine]] — `runtime.newproc` — точка входа создания горутины из компилятора
- [[go map]] — компилятор инициализирует seed хэш-функции для map
